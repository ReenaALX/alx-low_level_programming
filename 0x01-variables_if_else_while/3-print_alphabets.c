#include <stdio.h>

/**
 * main - main
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char x;

	for (x = 'a' ; x <= 'z' ; X++)
		putchar(x);
	for (X = 'A' ; x <= 'Z' ; x++)
		putchar(X);
	putchar('\n');
	return (0);
}
